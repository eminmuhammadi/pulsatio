package config

// Constants
const Name = "pulsatio"
const Usage = "pulsatio is a simple gRPC based remote terminal application."

// Mutable variables
var (
	Version       = "0.0.1"
	ServerTimeout = 13 * 1000
)
